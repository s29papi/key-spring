[build]
builder = "nixpacks"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "always"
restartPolicyMaxRetries = -1
numReplicas = 1
# Prevent Railway from sleeping the service
sleepApplication = false
